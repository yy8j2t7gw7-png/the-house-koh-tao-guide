# Meta Staff Quick Actions — v5.11.19 (Superseded)

Do not use the v5.11.19 action-template plan for production activation.

The service template originally named `house_service_alert_actions_v1` was created in Meta without the required **Received** and **Resolve** buttons. It must never be mapped. Quick actions remain disabled.

Use `META_STAFF_QUICK_ACTIONS_v5.11.20.md` for the corrected intended template set. That guide requires `house_service_alert_actions_v2` and permits activation only as a separate, explicitly authorized change after all five intended templates are confirmed Active.
